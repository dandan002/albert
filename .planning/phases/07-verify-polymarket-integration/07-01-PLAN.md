---
phase: 07-verify-polymarket-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md"
autonomous: true
requirements:
  - PM-01
  - PM-02
  - PM-03
must_haves:
  truths:
    - "07-VERIFICATION.md exists with evidence of SDK-based ECDSA authentication"
    - "07-VERIFICATION.md documents adapter registration in execution engine"
    - "07-VERIFICATION.md documents ingestor wiring and startup spawning"
  artifacts:
    - path: ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md"
      provides: "Formal verification evidence for Phase 1 Polymarket integration"
      min_lines: 40
      contains:
        - "PM-01"
        - "PM-02"
        - "PM-03"
        - "py-clob-client"
        - "ClobClient"
  key_links:
    - from: "07-VERIFICATION.md"
      to: "albert/execution/adapters/polymarket.py"
      via: "code inspection evidence"
      pattern: "_create_client|ClobClient"
    - from: "07-VERIFICATION.md"
      to: "albert/__main__.py"
      via: "registration and spawn evidence"
      pattern: "adapters\[.polymarket.\]|polymarket_ingestor"
---

<objective>
Create a formal verification artifact (07-VERIFICATION.md) that provides auditable evidence that Phase 1 Polymarket integration satisfies requirements PM-01, PM-02, and PM-03.

Purpose: Close the documentation gap identified in the v1.0 milestone audit by producing a traceable verification record.
Output: `07-VERIFICATION.md` with requirement-to-evidence mapping.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-polymarket-production-readiness/1-CONTEXT.md
@.planning/phases/01-polymarket-production-readiness/01-01-SUMMARY.md
@albert/execution/adapters/polymarket.py
@albert/ingestor/polymarket.py
@albert/execution/engine.py
@albert/__main__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect source code and verify PM-01, PM-02, PM-03 compliance</name>
  <files>
    albert/execution/adapters/polymarket.py
    albert/ingestor/polymarket.py
    albert/execution/engine.py
    albert/__main__.py
  </files>
  <read_first>
    - albert/execution/adapters/polymarket.py — verify SDK auth usage
    - albert/ingestor/polymarket.py — verify ingestor implementation
    - albert/execution/engine.py — verify adapter lookup mechanism
    - albert/__main__.py — verify registration and spawn
    - .planning/phases/01-polymarket-production-readiness/1-CONTEXT.md — locked decisions D-01, D-02, D-03
  </read_first>
  <action>
    Perform code inspection to gather specific evidence for each requirement:

    **PM-01 (SDK ECDSA Authentication):**
    - Confirm `polymarket.py` imports `ClobClient` from `py_clob_client.client`
    - Confirm `_create_client()` instantiates `ClobClient(host, key=key, chain_id=chain_id)`
    - Confirm `place_order()` calls `self._client.create_order()` and `self._client.post_order()`
    - Note line numbers for each finding
    - Verify `py-clob-client` package is installed in the environment

    **PM-02 (Adapter Registration):**
    - Confirm `__main__.py` creates `adapters["polymarket"] = PolymarketAdapter()` (lines 88-93)
    - Confirm `ExecutionEngine` receives the `adapters` dict
    - Confirm `engine._handle_intent()` resolves `exchange = intent.market_id.split(":")[0]` and calls `self._adapters.get(exchange)`
    - Confirm this lookup returns the PolymarketAdapter when market_id starts with "polymarket:"

    **PM-03 (Ingestor Wiring):**
    - Confirm `__main__.py` instantiates `PolymarketIngestor(bus, market_ids, shutdown_event=shutdown_event)` (line 98)
    - Confirm `polymarket_task = asyncio.create_task(polymarket_ingestor.run())` (line 104)
    - Confirm `PolymarketIngestor._connect_and_stream()` connects to `wss://ws-subscriptions-clob.polymarket.com/ws/market`
    - Confirm `_normalize()` publishes `MarketDataEvent` to `market_data` channel

    Record all findings with exact file paths, line numbers, and code snippets.
  </action>
  <verify>
    <automated>
      grep -n "from py_clob_client.client import ClobClient" albert/execution/adapters/polymarket.py && \
      grep -n "adapters\[.polymarket.\] = PolymarketAdapter()" albert/__main__.py && \
      grep -n "polymarket_ingestor = PolymarketIngestor" albert/__main__.py && \
      grep -n "self._adapters.get(exchange)" albert/execution/engine.py && \
      pip show py-clob-client | grep "Name: py_clob_client"
    </automated>
  </verify>
  <done>
    All three requirements (PM-01, PM-02, PM-03) are verified with specific line numbers and code snippets from source files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 07-VERIFICATION.md with evidence and traceability</name>
  <files>
    .planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/01-polymarket-production-readiness/1-CONTEXT.md — decision traceability
    - .planning/phases/01-polymarket-production-readiness/01-01-SUMMARY.md — prior verification results
    - .planning/REQUIREMENTS.md — requirement descriptions
  </read_first>
  <action>
    Write `.planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md` with the following structure:

    **Frontmatter:**
    ```yaml
    ---
    status: verified
    phase: 07-verify-polymarket-integration
    target_phase: 01-polymarket-production-readiness
    date: [current date]
    verifier: automated + code inspection
    ---
    ```

    **Sections:**
    1. **Scope** — What is being verified (PM-01, PM-02, PM-03 for Phase 1 Polymarket integration)
    2. **Methodology** — Code inspection + import verification + package presence check
    3. **Verification Matrix** — Table with columns: Requirement, Evidence Location, Finding, Line Numbers, Status
       - PM-01: polymarket.py lines 12-31 (ClobClient creation), lines 42-67 (order signing and posting)
       - PM-02: __main__.py lines 85-93 (adapter dict insertion), engine.py lines 58-63 (exchange lookup)
       - PM-03: __main__.py lines 97-104 (ingestor instantiation and task spawn), ingestor/polymarket.py lines 16-43 (WebSocket connection and normalization)
    4. **Decision Traceability** — Map to Phase 1 CONTEXT.md decisions:
       - D-01 (Use py-clob-client) → verified in PM-01 evidence
       - D-02 (Fail gracefully) → verified in __main__.py try/except block lines 89-93
       - D-03 (Register both WebSocket and REST) → verified in PM-02 and PM-03 evidence
    5. **Environment Verification** — Note py-clob-client version (0.34.6) and Python import success
    6. **Conclusion** — All requirements verified; Phase 1 Polymarket integration meets specifications

    Use exact line numbers from the current codebase. Include code snippets (3-5 lines each) for key evidence.
  </action>
  <verify>
    <automated>
      test -f ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md" && \
      grep -c "PM-01" ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md" | grep -q "[1-9]" && \
      grep -c "PM-02" ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md" | grep -q "[1-9]" && \
      grep -c "PM-03" ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md" | grep -q "[1-9]" && \
      grep -c "py-clob-client" ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md" | grep -q "[1-9]" && \
      wc -l ".planning/phases/07-verify-polymarket-integration/07-VERIFICATION.md" | awk '{print $1}' | xargs -I {} sh -c 'test {} -ge 40'
    </automated>
  </verify>
  <done>
    07-VERIFICATION.md exists, is at least 40 lines, references all three requirements (PM-01, PM-02, PM-03), references py-clob-client, and includes specific line numbers and code snippets.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification doc → public repo | Verification artifact must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | 07-VERIFICATION.md | mitigate | Verify no POLYMARKET_PRIVATE_KEY values, no API key strings, and no .env contents are copied into the verification document |
| T-07-02 | Tampering | 07-VERIFICATION.md | accept | Document is advisory only; code is the source of truth. Low impact if document is inaccurate. |
</threat_model>

<verification>
- 07-VERIFICATION.md exists and is readable
- File contains evidence for PM-01, PM-02, PM-03
- File references specific line numbers from polymarket.py, __main__.py, engine.py, and ingestor/polymarket.py
- File maps evidence to Phase 1 CONTEXT.md decisions (D-01, D-02, D-03)
</verification>

<success_criteria>
- VERIFICATION.md artifact created in Phase 7 directory
- Artifact documents SDK authentication (PM-01), adapter registration (PM-02), and ingestor wiring (PM-03)
- Artifact includes traceability to Phase 1 locked decisions
</success_criteria>

<output>
After completion, create `.planning/phases/07-verify-polymarket-integration/07-01-SUMMARY.md`
</output>
