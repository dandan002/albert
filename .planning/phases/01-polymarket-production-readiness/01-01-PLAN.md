---
phase: 01-polymarket-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - albert/execution/adapters/polymarket.py
  - albert/ingestor/polymarket.py
  - albert/__main__.py
autonomous: true
requirements:
  - PM-01
  - PM-02
  - PM-03
must_haves:
  truths:
    - "PolymarketAdapter uses official SDK for ECDSA authentication"
    - "Adapter registered in execution engine at startup"
    - "WebSocket market data flows to strategy engine"
  artifacts:
    - path: "albert/execution/adapters/polymarket.py"
      provides: "PolymarketAdapter with SDK auth"
    - path: "albert/ingestor/polymarket.py"
      provides: "PolymarketIngestor for market data"
  key_links:
    - from: "PolymarketAdapter"
      to: "py-clob-client"
      via: "PyClobClient.create_from_env()"
    - from: "__main__.py"
      to: "PolymarketAdapter"
      via: "adapters dict registration"
---

<objective>
Implement Polymarket ECDSA authentication using official SDK and register adapter in execution engine.

Purpose: Fix critical authentication blocker so Polymarket orders can execute successfully
Output: Working PolymarketAdapter with SDK integration and registered adapter
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-polymarket-production-readiness/1-CONTEXT.md
@albert/execution/adapters/kalshi.py
@albert/execution/adapters/base.py
</context>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Install py-clob-client SDK</name>
  <files>pyproject.toml</files>
  <action>
Add dependency: `py-clob-client>=0.20.0` to dependencies section in pyproject.toml.

Run: `pip install -e .` to install the package with new dependency.
  </action>
  <verify>
<automated>grep -q "py-clob-client" pyproject.toml && pip show py-clob-client | grep -q "Version:"</automated>
  </verify>
  <done>py-clob-client installed and importable</done>
</task>

<task type="auto">
  <name>Update PolymarketAdapter with SDK authentication</name>
  <files>albert/execution/adapters/polymarket.py</files>
  <read_first>
albert/execution/adapters/polymarket.py
albert/execution/adapters/kalshi.py
albert/execution/adapters/base.py
</read_first>
  <action>
Rewrite PolymarketAdapter to use py-clob-client for authentication:

1. Import: `from py_clob_client.client import PyClobClient`
2. Replace manual auth with: `client = PyClobClient.create_from_env()`
3. Keep existing interface: `place_order()`, `cancel_order()`, `get_bankroll()` - delegate to client
4. Implement retry with exponential backoff (3 attempts, same as D-02 in CONTEXT.md)
5. On auth failure: log error, mark adapter as disabled, allow continued operation with other exchanges

Reference patterns from kalshi.py for retry logic and error handling.
  </action>
  <verify>
<automated>python -c "from albert.execution.adapters.polymarket import PolymarketAdapter; print('import ok')"</automated>
  </verify>
  <done>PolymarketAdapter uses py-clob-client SDK, handles auth failures gracefully</done>
</task>

<task type="auto">
  <name>Register PolymarketAdapter in execution engine</name>
  <files>albert/__main__.py</files>
  <read_first>
albert/__main__.py
</read_first>
  <action>
In `albert/__main__.py`, around line 62 where adapters are registered:

1. Import PolymarketAdapter
2. Instantiate when env vars present: `PyClobClient.get_env_vars()` checks for API key
3. Register in adapter dict: `adapters["polymarket"] = PolymarketAdapter()`
4. Start PolymarketIngestor for market data if not already running

Also update ExecutionEngine to use the registered adapters dict.
  </action>
  <verify>
<automated>python -c "from albert.__main__ import run; print('registration loads')"</automated>
  </verify>
  <done>PolymarketAdapter registered and available at startup</done>
</task>

<task type="auto">
  <name>Verify WebSocket ingestor works</name>
  <files>albert/ingestor/polymarket.py</files>
  <read_first>
albert/ingestor/polymarket.py
albert/ingestor/base.py
</read_first>
  <action>
Verify PolymarketIngestor connects to WebSocket endpoint and normalizes data:

1. Check existing implementation - may already work
2. If SDK changes affected API, update normalization logic
3. Test WebSocket connection (may need staging credentials)

Ensure no import errors and WebSocket connects.
  </action>
  <verify>
<automated>python -c "from albert.ingestor.polymarket import PolymarketIngestor; print('ingestor loads')"</automated>
  </verify>
  <done>PolymarketIngestor loads without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Exchange | Untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-------------|----------------|
| T-01 | Tampering | Order parameters | mitigate | Validate all order params (side, price, size) before sending to SDK |
| T-02 | Repudiation | Order submission | mitigate | Log all order attempts with trace IDs for audit trail |
</threat_model>

<verification>
All tasks complete: SDK installed, adapter updated, adapter registered, ingestor verified
</verification>

<success_criteria>
PM-01: PolymarketAdapter uses py-clob-client for ECDSA authentication
PM-02: PolymarketAdapter registered in execution engine
PM-03: PolymarketWebSocket ingestor loads without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-polymarket-production-readiness/01-01-SUMMARY.md`
</output>