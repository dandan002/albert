---
phase: 05-fix-critical-resilience-bugs
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/test_integration.py
autonomous: true
requirements:
  - RES-01
  - RES-03
must_haves:
  truths:
    - "Integration test proves all tasks exit within 5 seconds of shutdown_event.set()"
    - "Integration test proves circuit breaker publishes StrategyHaltedEvent when daily loss limit is exceeded"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "Integration tests for shutdown and circuit breaker"
      pattern: "test_graceful_shutdown|test_circuit_breaker"
  key_links:
    - from: "tests/test_integration.py"
      to: "albert/__main__.py"
      via: "pytest verifies shutdown logic"
      pattern: "shutdown_event.set|asyncio.wait_for"
    - from: "tests/test_integration.py"
      to: "albert/execution/risk.py"
      via: "pytest verifies circuit breaker publishes event"
      pattern: "bus.subscribe.*strategy_halted|await checker.check"
---

<objective>
Add integration tests that verify the two critical resilience fixes from Plan 01:
1. All asyncio tasks exit cleanly within 5 seconds when shutdown_event is set
2. Circuit breaker publishes StrategyHaltedEvent when daily loss limit violations exceed threshold

Purpose: Provide automated proof that the gap-closure fixes work end-to-end.
Output: Passing integration tests in tests/test_integration.py.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-critical-resilience-bugs/05-01-PLAN.md

<interfaces>
From albert/events.py:
```python
@dataclass
class StrategyHaltedEvent:
    strategy_id: str
    reason: str
    timestamp: datetime
```

From albert/execution/risk.py (after Plan 01):
```python
class RiskChecker:
    async def check(self, intent: OrderIntent, position_size_usd: float) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration tests for shutdown and circuit breaker</name>
  <files>tests/test_integration.py</files>
  <behavior>
    - Test 1: Start all engine tasks with shutdown_event, set it, verify all tasks complete within 5s
    - Test 2: Seed DB with daily_pnl below limit, create RiskChecker with EventBus, exceed violations, verify StrategyHaltedEvent is delivered
  </behavior>
  <action>
Add two new integration tests to `tests/test_integration.py`.

**Test 1: `test_graceful_shutdown_cancels_all_tasks_within_5_seconds`**
- Create an `EventBus` and `shutdown_event = asyncio.Event()`
- Create an in-memory SQLite DB, migrate it, insert one enabled strategy
- Create mock adapter with `AsyncMock` for `place_order` and `get_bankroll`
- Instantiate and start tasks for:
  - `StrategyEngine`
  - `ExecutionEngine`
  - `PortfolioTracker`
  - Use `asyncio.create_task()` for each
- Wait 0.1s for tasks to start
- Set `shutdown_event.set()`
- Use `asyncio.wait_for(asyncio.gather(*tasks, return_exceptions=True), timeout=5.0)`
- Assert no `asyncio.TimeoutError` is raised (the gather completes within 5s)
- Assert all tasks are done (`task.done() for task in tasks`)

**Test 2: `test_circuit_breaker_publishes_strategy_halted_event`**
- Create an `EventBus`, subscribe to `"strategy_halted"` channel
- Create an in-memory SQLite DB, migrate it
- Insert a row into `daily_pnl` with `realized_pnl + unrealized_pnl < daily_loss_limit` for today
- Create `RiskChecker(conn, global_config, bus)` with:
  - `daily_loss_limit_usd=-100.0`
  - `circuit_breaker_violations=2`
  - `order_debounce_seconds=0`
- Create an `OrderIntent` with `strategy_id="s1"`
- Call `await checker.check(intent, position_size_usd=10.0)` repeatedly:
  - 1st call: should return False, violation count = 1 (no event yet)
  - 2nd call: should return False, violation count = 2, publishes `StrategyHaltedEvent`
- Use `asyncio.wait_for(halted_queue.get(), timeout=1.0)` to receive the event
- Assert the received event is a `StrategyHaltedEvent`
- Assert `event.strategy_id == "s1"`
- Assert `"circuit_breaker" in event.reason`

**Important patterns to follow:**
- Use the existing `make_db_with_strategy` / `make_mock_adapter` patterns from `test_execution_engine.py` if helpful
- Keep `order_debounce_seconds=0` in global_config to avoid debounce interference
- Use `asyncio.wait_for` with short timeouts so tests fail fast if bugs regress
- Do NOT test real WebSocket connections — mock everything
  </action>
  <verify>
    <automated>python -m pytest tests/test_integration.py::test_graceful_shutdown_cancels_all_tasks_within_5_seconds tests/test_integration.py::test_circuit_breaker_publishes_strategy_halted_event -x -v</automated>
  </verify>
  <done>
    - Both new integration tests pass
    - `test_graceful_shutdown_cancels_all_tasks_within_5_seconds` completes without TimeoutError
    - `test_circuit_breaker_publishes_strategy_halted_event` receives StrategyHaltedEvent with correct strategy_id and reason
    - All existing tests in `test_integration.py` still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → System under test | Tests exercise internal components, no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | Long-running integration test | mitigate | `asyncio.wait_for(..., timeout=5.0)` in tests ensures fast failure; mock all I/O |
</threat_model>

<verification>
- Run `python -m pytest tests/test_integration.py -x -v` — all pass
- Run `python -m pytest tests/ -x -v` — full suite passes (no regressions)
</verification>

<success_criteria>
- `test_graceful_shutdown_cancels_all_tasks_within_5_seconds` passes, verifying clean shutdown
- `test_circuit_breaker_publishes_strategy_halted_event` passes, verifying event delivery
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-critical-resilience-bugs/05-02-SUMMARY.md`
</output>
