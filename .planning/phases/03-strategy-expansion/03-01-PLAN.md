---
phase: 03-strategy-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - albert/strategies/examples/mean_reversion.py
  - albert/strategies/examples/momentum.py
  - albert/strategies/engine.py
autonomous: true
requirements:
  - STR-01
  - STR-02
  - STR-03
must_haves:
  truths:
    - "MeanReversionStrategy implements mean reversion with edge calculation"
    - "Momentum strategy loads from configuration"
    - "Strategies emit orders only when edge exceeds configured threshold"
  artifacts:
    - path: "albert/strategies/examples/mean_reversion.py"
      provides: "MeanReversionStrategy with edge calculation"
    - path: "albert/strategies/examples/momentum.py"
      provides: "Updated MomentumV1 with edge threshold"
key_links:
  - from: "StrategyEngine"
    to: "BaseStrategy"
    via: "class加载from_db"
  - from: "kelly_size"
    to: "OrderIntent"
    via: "confidence参数"
---

<objective>
Implement mean reversion and momentum strategies with edge calculation.

Purpose: Users can load strategies from configuration that emit orders only when edge exceeds threshold
Output: Two strategies with proper edge calculation and threshold-based order emission
</objective>

<context>
@.planning/ROADMAP.md
@albert/strategies/base.py
@albert/strategies/engine.py
@albert/strategies/examples/momentum.py
@albert/execution/kelly.py
</context>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Implement MeanReversionStrategy</name>
  <files>albert/strategies/examples/mean_reversion.py</files>
  <read_first>
albert/strategies/base.py
albert/strategies/examples/momentum.py
  </read_first>
  <action>
Create MeanReversionStrategy that:
1. Extends BaseStrategy
2. Calculates edge as deviation from running mean of last N prices
3. Emits OrderIntent only when edge exceeds min_edge config
4. Uses config: window_size (default 20), min_edge (default 0.03)

Reference the momentum.py pattern for structure.
  </action>
  <verify>
<automated>python -c "from albert.strategies.examples.mean_reversion import MeanReversionStrategy; print('ok')"</automated>
  </verify>
  <done>MeanReversionStrategy imports and instantiates</done>
</task>

<task type="auto">
  <name>Update MomentumV1 with edge threshold</name>
  <files>albert/strategies/examples/momentum.py</files>
  <read_first>
albert/strategies/examples/momentum.py
  </read_first>
  <action>
Update MomentumV1 to:
1. Use kelly_size() for edge-to-confidence conversion
2. Add config: max_position_usd (default 1000), kelly_fraction (default 0.25)
3. Skip orders where kelly_size returns 0 (no positive edge)
  </action>
  <verify>
<automated>python -c "from albert.strategies.examples.momentum import MomentumV1; print('ok')"</automated>
  </verify>
  <done>MomentumV1 uses kelly_size for position sizing</done>
</task>

<task type="auto">
  <name>Verify strategies load from DB</name>
  <files>albert/strategies/engine.py</files>
  <read_first>
albert/strategies/engine.py
  </read_first>
  <action>
Verify StrategyEngine loads strategies from DB with class_path:
- albert.strategies.examples.momentum.MomentumV1
- albert.strategies.examples.mean_reversion.MeanReversionStrategy

Ensure dynamic import works for both.
  </action>
  <verify>
<automated>python -c "import importlib; m = importlib.import_module('albert.strategies.examples.mean_reversion'); print('import ok')"</automated>
  </verify>
  <done>Strategies dynamically importable from DB class_path</done>
</task>

</tasks>

<verification>
All tasks complete: MeanReversionStrategy implemented, MomentumV1 updated, strategies loadable
</verification>

<success_criteria>
STR-01: User can load mean reversion strategy from configuration
STR-02: User can load momentum strategy from configuration
STR-03: Strategies emit orders only when calculated edge exceeds configured threshold
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-expansion/03-01-SUMMARY.md`
</output>

(End of file - total 133 lines)